# Pinned to the versions this site is known to build with.
mkdocs-material==9.7.7
mkdocs-awesome-nav==3.3.0
PyYAML==6.0.3

# Renders docs/remote-interface/openapi.yaml. Vendors Swagger UI itself, so the
# built site loads no scripts from a CDN.
mkdocs-swagger-ui-tag==0.8.1

# tools/hooks.py validates openapi.yaml during the build, so an invalid spec
# fails `mkdocs build --strict` instead of producing a broken reference page.
openapi-spec-validator==0.9.0

# RELAX NG validation and Schematron execution for the validators that
# tools/generate_validators.py derives from spec/*.yml during the build.
lxml==6.1.2

# Independent parser for the RELAX NG *compact* artifact - the .rnc emitter in
# tools/generate_validators.py is hand-rolled string code, and this is the only
# thing that would notice it emitting syntactically broken output.
rnc2rng==2.7.0
